// Mirrors the backend's src/players/EnemyPreview.ts enum. Gaps reserved for future
// reveal talents ("increasingly reveal more info about your next opponent").
export enum EnemyRevealLevel {
  IDENTITY = 0,
  FULL = 100,
}
